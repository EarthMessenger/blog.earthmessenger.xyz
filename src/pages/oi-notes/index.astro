---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

const allPosts = await getCollection("solution");

const title="題解";
---

<BaseLayout title={title}>
  <h1>{title}</h1>
  <ul>
    {
      allPosts.map((post) => (
        <li>
          <a href={`/oi-notes/${post.id}`}>{post.data.title}</a>
        </li>
      ))
    }
  </ul>
</BaseLayout>
